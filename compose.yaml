services:
  backend:
    build:
      context: ./backend
    container_name: barber-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: barber-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend